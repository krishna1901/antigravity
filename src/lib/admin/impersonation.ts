/** Cookie names for the admin impersonation flow (plain module so both server
 *  actions and server components can import them). */
export const IMP_ADMIN_COOKIE = "sf_imp_admin";
export const IMP_ACTIVE_COOKIE = "sf_imp_active";
